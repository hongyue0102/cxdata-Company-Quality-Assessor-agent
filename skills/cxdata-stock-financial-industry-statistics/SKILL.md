---
name: stock-financial-industry-statistics
description: 存储上市公司重要财务指标在公司所属的申万行业分类体系中的排名情况，以及按申万行业分类体系下的不同级别分别统计计算的上市公司主要财务衍生指标的均值、中值数据。主要包括：扣非归母净利润、每股净资产、销售净利率、存货周转率、流动比率、现金比率、货币资金、交易性金融资产、资产总计、负债合计、少数股东权益等。
metadata:
  version: "1.0.0"
  author: "财新数据"
  website: "guizhi.io"
  tags: ["bs-industry-stats", "is-industry-stats", "cfs-industry-stats", "stock-financial"]
---

# 执行标准程序 (Recommended Workflow)

当用户发起查询时，AI 推荐按照以下步骤执行：

1. **Step 1: 调用统一工具**
- 使用 `python ./scripts/api_query.py <API_ID> key=value [key=value] ...` 命令获取接口数据
- 工具会自动完成：加载配置 → 获取token → 发送请求 → 解压数据 → 输出JSON结果
- **调用方式**：所有接口统一使用 key=value 格式传参

2. **Step 2: 解析结果**
- 工具输出为格式化的JSON数据，直接解析并呈现给用户

# 数据处理说明

## 配置说明
本技能所有接口需要认证token，配置方式：
1. 在 `./scripts/.env` 文件中配置 `USER_KEY` 和 `BASE_URL`
2. 调用 `./scripts/api_query.py` 时会自动获取并使用token

## 脚本功能
`./scripts/api_query.py` 脚本功能：
- 自动加载 `./scripts/.env` 配置文件
- 自动获取 `authtoken`
- 构建请求URL并发送GET请求
- 自动处理 Base64 解码和 Gzip 解压
- 输出格式化的JSON结果

# 使用场景

- 用户查询按不同行业分类标准统计的各行业、各报告期的财务分析指标数据。主要包括：行业代码、行业名称、行业流动资产、行业总资产、行业平均总资产、行业负债总额、行业营业利润等
- 用户查询上市公司重要财务指标在公司所属的申万行业分类体系中的排名情况。主要包括：股票代码、股票简称、截止日期、扣非归母净利润、每股净资产、销售净利率、存货周转率、流动比率、现金比率等
- 用户查询按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）现金流量表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、销售商品、提供劳务收到的现金、经营活动现金流入小计、经营活动产生的现金流量净额、偿还债务支付的现金、筹资活动产生的现金流量净额等
- 用户查询按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）资产负债表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、货币资金、交易性金融资产、资产总计、负债合计、少数股东权益等
- 用户查询按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）利润表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、营业收入、营业成本、营业税金及附加、	业务及管理费、利润总额、净利润等
- 用户查询按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）主要财务衍生指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、扣非归母净利润、每股净资产、市盈率PE、市销率PB、产权比率、流动比率等



## 调用方式

所有接口统一使用 `api_query.py` 工具调用，参数采用 `key=value` 格式：

```bash
python ./scripts/api_query.py <API_ID> key=value [key=value] ...
```

## 接口清单
> ⚠️ **注意事项**：
> 1. 所有接口输入输出需要严格按照接口文档规范
> 2. 所有接口标识必须严格按照 API_ID 进行请求，不得杜撰不存在的接口
> 3. 调用接口之前必须阅读接口文档

| 接口名称 | 接口文档 | API_ID | 接口描述 |
|----------|----------|--------|----------|
| 上市公司财务指标行业排名-通用 | ./references/getStkFinIndexInduRankByCond-G.md | getStkFinIndexInduRankByCond-G | 存储上市公司重要财务指标在公司所属的申万行业分类体系中的排名情况。主要包括：股票代码、股票简称、截止日期、扣非归母净利润、每股净资产、销售净利率、存货周转率、流动比率、现金比率等。 |
| 利润表行业统计-通用 | ./references/getStkFinProfTapbInduByCond-G.md | getStkFinProfTapbInduByCond-G | 存储按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）利润表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、营业收入、营业成本、营业税金及附加、	业务及管理费、利润总额、净利润等。 |
| 现金流量表行业统计-通用 | ./references/getStkFinCashTacbInduByCond-G.md | getStkFinCashTacbInduByCond-G | 存储按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）现金流量表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、销售商品、提供劳务收到的现金、经营活动现金流入小计、经营活动产生的现金流量净额、偿还债务支付的现金、筹资活动产生的现金流量净额等。 |
| 行业财务分析指标-通用 | ./references/getStkInduFinIndexByCond-G.md | getStkInduFinIndexByCond-G | 存储按不同行业分类标准统计的各行业、各报告期的财务分析指标数据。主要包括：行业代码、行业名称、行业流动资产、行业总资产、行业平均总资产、行业负债总额、行业营业利润等。 |
| 财务衍生指标行业统计-通用 | ./references/getStkFinDeriIndexInduByCond-G.md | getStkFinDeriIndexInduByCond-G | 存储按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）主要财务衍生指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、扣非归母净利润、每股净资产、市盈率PE、市销率PB、产权比率、流动比率等。 |
| 资产负债表行业统计-通用 | ./references/getStkFinBalaTabbInduByCond-G.md | getStkFinBalaTabbInduByCond-G | 存储按申万行业分类体系下的不同级别分别统计计算的上市公司（不含三板）资产负债表指标的均值、中值数据。主要包括：行业名称、行业级别、截止日期、货币资金、交易性金融资产、资产总计、负债合计、少数股东权益等。 |



### API_ID 参数

- `API_ID` 用于指定要调用的具体接口，根据查询类型选择
- 示例API_ID：
- `getStkFinIndexInduRankByCond-G`: 上市公司财务指标行业排名-通用
- `getStkFinProfTapbInduByCond-G`: 利润表行业统计-通用
- `getStkFinCashTacbInduByCond-G`: 现金流量表行业统计-通用
- 更多API_ID请参考接口[接口清单](#接口清单)

## 故障排除

- **调用失败**：检查 `./scripts/.env` 文件是否存在并正确配置了 `CXDA_USER_KEY` 和 `BASE_URL`，确认网络连接正常，token未过期。
- **输出为空**：确认输入参数是否正确，检查API_ID是否匹配查询类型。
- **权限问题**：接口返回无权限或者权限到期时，提示用户前往`https://yun.ccxe.com.cn/`联系客服
- **scripts路径问题**：文档中描述的是skill相对路径，请求时确认路径是否正确
- **相同含义字段说明**：ORG_UNI_CODE==COM_UNI_CODE;STK_UNI_CODE==BOND_UNI_CODE；如无明确说明股票代码不需要携带SH、HK等交易所代码
- **接口输入字段不明确**：接口输入参数是ORG_UNI_CODE、STK_UNI_CODE等参数时可以先通过对应的基础接口调用尝试获取，获取不到时反馈并停止
- **其他异常**：出现其他异常时停止输出并返回异常原因
